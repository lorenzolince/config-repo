server:
  port: 8080 #the default port used for zuul service
  
zuul:
  ignoredServices: '*'
  book:
   api: /api/**
   url: http://local-server:8088
   stripPrefix: false

ribbon:
  eureka:
    enabled: false
